# 核心原则：节省 Token 与精准输出
- 始终使用 Diff 格式提供代码修改，严禁全量输出已有文件内容。
- 除非我显式要求，否则不要解释基础语法或已知的逻辑。
- 针对所有代码，优先使用类型注解，保持代码简洁，减少冗余注释。
- 在进行 Web Scraping 开发时，只关注提取逻辑，不要生成无用的数据处理示例。
- 严禁在回答中重复我提供的 Context（上下文）内容。


# 排除虚拟环境和缓存
venv/
.venv/
__pycache__/
*.pyc

# 排除爬虫抓取的数据（Token 消耗大户）
*.json
*.csv
*.log
data/
output/

# 排除隐藏的 Git 或 IDE 配置
.git/
.idea/
.vscode/